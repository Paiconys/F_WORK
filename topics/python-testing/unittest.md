# Unittest

## Коротко

Встроенный xUnit-фреймворк на классах `TestCase`. 

## Развёрнуто

`setUp`/`tearDown`, `assertEqual`, discovery через `python -m unittest`. Хорош для легаси и стандартной библиотеки. Для нового кода часто берут pytest.
